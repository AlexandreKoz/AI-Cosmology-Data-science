# Benchmarks

Benchmark targets should follow `bench_<topic>` naming and remain independently runnable.

Current hook:

- `bench_layout_smoke`: validates build wiring and provides a lightweight SoA traversal timing baseline.
